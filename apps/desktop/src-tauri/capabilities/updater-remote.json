{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater-remote",
  "description": "Allows the remote web UI (nora.systems, loaded in the main window) to check and install signed updates, and restart the app afterwards. Minimal scope: only updater + restart. Endpoints and pubkey are fixed in tauri.conf.json, so remote content cannot redirect the update source.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://nora.systems/*"]
  },
  "permissions": [
    "updater:allow-check",
    "updater:allow-download-and-install",
    "process:allow-restart"
  ]
}
