# NORA (raiz) — variaveis usadas pela infra local (docker compose).
# Copie para `/.env.local` e ajuste. Nunca commite `.env.local`.

# --- Postgres (infra/docker/docker-compose.yml) ---
POSTGRES_DB=nora
POSTGRES_USER=nora
POSTGRES_PASSWORD=nora_dev

# Observacao: variaveis de API/worker/web ficam nos respectivos:
# - services/api/.env.example
# - services/nlp-worker/.env.example
# - apps/web/.env.example
