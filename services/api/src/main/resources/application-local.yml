# Perfil local de desenvolvimento. Ativado por SPRING_PROFILES_ACTIVE=local.
spring:
  jpa:
    show-sql: false
nora:
  security:
    expose-dev-tokens: true   # retorna tokens de verificacao/reset no JSON para testes manuais
logging:
  level:
    br.com.nora: DEBUG
